/** @type {import('next').NextConfig} */
    const nextConfig = {
      images: {
        domains: ['fonts.googleapis.com', 'fonts.googleapis.com'],
      },
    };
    
    export default nextConfig;